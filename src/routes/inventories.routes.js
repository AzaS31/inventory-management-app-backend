// import { Router } from "express";
// import * as controller from "../api/inventories/inventories.controller.js";
// import { isAuthenticated } from "../middlewares/isAuthenticated.js";

// const router = Router();

// router.get("/", isAuthenticated, controller.getAll);
// router.get("/mine", isAuthenticated, controller.getMine);
// router.post("/", isAuthenticated, controller.create);
// router.put("/:id", isAuthenticated, controller.update);
// router.delete("/:id", isAuthenticated, controller.remove);

// export default router;
